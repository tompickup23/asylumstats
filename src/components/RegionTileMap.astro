---
import { buildPublicPlaceRegionPath } from "../lib/site";
import {
  REGION_MAP_DRAW_ORDER,
  REGION_MAP_HEIGHT,
  REGION_MAP_OUTLINES,
  REGION_MAP_WIDTH,
  getRegionMapGeometry
} from "../lib/region-map";

interface RegionValue {
  regionName: string;
  value: number;
  note?: string;
}

interface Props {
  items: RegionValue[];
  title: string;
  description?: string;
  valueLabel: string;
  highlightRegion?: string;
  highlightRegions?: string[];
  tone?: "accent" | "teal" | "warm";
}

const { items, title, description, valueLabel, highlightRegion, highlightRegions = [], tone = "accent" } = Astro.props;

const itemByRegion = new Map(items.map((item) => [item.regionName, item]));
const renderedRegions = REGION_MAP_DRAW_ORDER.flatMap((regionName) => {
  const geometry = getRegionMapGeometry(regionName);
  const item = itemByRegion.get(regionName);

  if (!geometry || !item) {
    return [];
  }

  return [{ geometry, item }];
});
const values = items.map((item) => item.value);
const minValue = Math.min(...values, 0);
const maxValue = Math.max(...values, 1);
const range = Math.max(maxValue - minValue, 1);
const activeRegions = new Set([highlightRegion, ...highlightRegions].filter(Boolean));

function formatValue(value: number): string {
  return new Intl.NumberFormat("en-GB", {
    minimumFractionDigits: Number.isInteger(value) ? 0 : 1,
    maximumFractionDigits: Number.isInteger(value) ? 0 : 2
  }).format(value);
}

function getFill(value: number): string {
  const ratio = (value - minValue) / range;

  if (tone === "teal") {
    return `rgba(78, 228, 195, ${0.18 + ratio * 0.56})`;
  }

  if (tone === "warm") {
    return `rgba(255, 139, 97, ${0.18 + ratio * 0.56})`;
  }

  return `rgba(125, 211, 252, ${0.18 + ratio * 0.56})`;
}
---

<figure class={`viz-card tone-${tone}`}>
  <div class="viz-head">
    <div>
      <h3>{title}</h3>
      {description && <p class="tiny">{description}</p>}
    </div>
    <span class="chip">{valueLabel}</span>
  </div>
  <svg class="region-map" viewBox={`0 0 ${REGION_MAP_WIDTH} ${REGION_MAP_HEIGHT}`} role="img" aria-label={title}>
    <path class="region-map-outline" d={REGION_MAP_OUTLINES.mainland}></path>
    <path class="region-map-outline" d={REGION_MAP_OUTLINES.northernIreland}></path>
    {renderedRegions.map(({ geometry, item }) => {
      const active = activeRegions.has(item.regionName);
      const href = buildPublicPlaceRegionPath(item.regionName);
      const valueLabelText = formatValue(item.value);

      return (
        <a
          href={href}
          class:list={["region-map-link", active && "is-active"]}
          aria-label={`${item.regionName}: ${valueLabelText} ${valueLabel}. Open region page.`}
        >
          <title>{`${item.regionName}: ${valueLabelText} ${valueLabel}${item.note ? `. ${item.note}` : ""}`}</title>
          <path
            class="region-map-region"
            d={geometry.path}
            fill={getFill(item.value)}
            stroke={active ? "#F5F7FB" : "rgba(255,255,255,0.16)"}
            stroke-width={active ? "3" : "1.4"}
          ></path>
          {geometry.calloutPath && <path class="region-map-callout" d={geometry.calloutPath}></path>}
          <text
            x={geometry.labelX}
            y={geometry.labelY}
            text-anchor={geometry.labelAnchor ?? "middle"}
            class:list={["region-map-label", active && "is-active"]}
          >
            {geometry.labelLines.map((line, index) => (
              <tspan x={geometry.labelX} dy={index === 0 ? 0 : 14}>
                {line}
              </tspan>
            ))}
          </text>
          <text
            x={geometry.valueX}
            y={geometry.valueY}
            text-anchor={geometry.labelAnchor ?? "middle"}
            class:list={["region-map-value", active && "is-active"]}
          >
            {valueLabelText}
          </text>
        </a>
      );
    })}
  </svg>
  <figcaption class="tiny">
    {activeRegions.size > 0 ? "Highlighted regions mark the current focus. " : ""}
    Select a region on the map to open its region page. Darker fills indicate higher values in the latest local-authority snapshot.
  </figcaption>
</figure>
